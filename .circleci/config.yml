version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Say Sakshi"
          command: "echo Hello, sakshi!"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello